---
inclusion: always
---
# 命名规范（同 steering/lugang-naming.md，供代码场景引用）

- 文件/目录：小写连字符（`hk-transport-plugin`）
- 组件：PascalCase（`AIResponseBox`）
- 函数/变量：camelCase（`geocodeWithFallback`）
- 常量：UPPER_SNAKE（`MTR_STATION_MAP`）
- 布尔：`is`/`has`/`enable` 前缀
- 事件处理：`handle` + 动作（`handleSubmit`）
