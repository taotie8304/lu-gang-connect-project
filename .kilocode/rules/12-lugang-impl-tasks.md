---
inclusion: fileMatch: ["TODO*", "TASKS*", "*.task.md"]
---
# 任务执行规范

## 执行原则
- 每次只做一件事，完成后确认再继续
- 修改前先理解现有代码（读文件再写）
- 不确定影响范围时，先列出将变更的文件

## 任务跟踪
- 在 `session-handoff.md` 中记录当前任务状态
- 完成后在 `project-memory.md` 追加到已完成列表
- 任务中断时立即更新 `session-handoff.md`
