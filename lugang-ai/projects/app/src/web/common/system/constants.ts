export enum TrackEventName {
  windowError = 'windowError',
  pageError = 'pageError',
  wordReadError = 'wordReadError'
}

// 鲁港通 - 客服/联系邮箱统一常量，供用户设置面板产品反馈、辅助使用设计弹窗等复用，避免各处硬编码。
export const LUGANG_SUPPORT_EMAIL = 'service@airscend.com';
