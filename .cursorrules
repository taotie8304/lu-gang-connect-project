# 鲁港通项目 - 全局开发与沟通规则（Cursor 版）

## 角色与语言
- 始终使用简体中文回复。
- 用户没有编程背景，解释技术问题时使用通俗易懂的语言，避免专业术语，如果必须使用请顺带解释含义。
- 沟通时要透明，说明你正在做什么、为什么这么做。

## 会话与记忆管理（模拟 Kiro Steering）
- 每次开始新的对话或第一次接手本项目时：
  - 先阅读 `project-memory.md` 和 `session-handoff.md`。
  - 用 1 段话向用户总结当前项目状态和待办任务，然后再开始具体工作。
- 绝不假装“记得之前的对话内容”，所有长期记忆以 `project-memory.md` 为准。
- 若这两个文件缺失或内容明显过时，应主动提醒用户并建议更新。

## 开发工作流（Spec-Driven，三步走）
对任何“新增功能”或“重要改动”，必须严格遵循以下步骤：

1. **设计阶段（Spec）**
   - 先不要写代码。
   - 阅读相关文件与 `project-memory.md`，理解当前架构。
   - 输出一份设计方案，包含：
     - 要修改或新增的文件列表。
     - 每个文件的修改点（函数、组件、接口）。
     - 数据流/请求流程的简要说明。
   - 用中文向用户确认：“以上方案是否确认？如确认，我将按此方案修改代码。”

2. **编码与验证阶段（Code & Test）**
   - 仅在用户确认方案后，才开始修改代码。
   - 修改完成后，默认使用以下命令运行测试：
     - `pnpm vitest run --config vitest.simple.config.mts`
   - 如果需要其他命令，应先征求用户确认。

3. **记忆更新阶段（Memory Update）**
   - 测试通过后，必须帮助用户更新：
     - `session-handoff.md`：记录本次 session 完成的工作、未解决问题和下一步计划。
     - `project-memory.md`：记录新的已完成功能和重要决策。
   - 在更新后，对用户说一句类似：“交接文件和长期记忆已更新，可以安全开始下一轮开发。”

## 上下文与代码库访问
- 避免无意义地“扫描整个代码库”，只在用户要求或确实有必要时才全局搜索。
- 若上下文长度接近模型极限，应主动建议用户结束当前对话并开启新对话，同时先整理/更新 `session-handoff.md`。

## 代码约定
- 所有代码注释使用 `// 鲁港通 - xxx` 格式。
- 绝不修改 `@fastgpt/*` 导入路径（这些是第三方依赖）。
- 包管理器固定使用 `pnpm`（这是一个 monorepo 项目）。
- 运行自动化测试统一使用：`pnpm vitest run --config vitest.simple.config.mts`，不要使用 `npx`。

## 终端与生产环境安全
- 默认把所有环境视为生产环境，禁止在未明确说明的情况下进行破坏性操作。
- 任何包含下列行为的命令，一律不得自动执行，只能给出建议并解释风险，由用户自行复制执行：
  - 删除文件或目录（rm -rf /, rm -rf *, rm -rf /www/..., 等）。
  - 删除或强制删除容器（docker rm -f ...）。
  - 直接修改数据库结构（DROP TABLE, TRUNCATE, DELETE FROM 无 WHERE 条件等）。
- 遇到涉及服务器 156.225.30.134 的操作时，必须先用自然语言解释你的打算，再给出命令。
- 用户如果只说“帮我部署一下”，必须先确认是“只更新前端”、“只更新后端”还是“两者都更新”，然后再按 deploy-guide.md 中既有流程给命令。

## 模型与隐私
- 优先使用支持中文、推理能力强的模型（如 Claude opus4.6 或 GPT-5.3），但禁止为了“试效果”随意切换模型导致行为不一致。
- 除非用户明确同意，禁止将完整仓库或大段业务代码发到任何外部第三方 API 或网站（包括在线格式化工具、未知代码分析服务等）。
- 如需使用联网搜索或第三方工具，只能用于查文档、查错误含义、查官方说明，不得上传含有秘钥、数据库连接字符串或敏感业务逻辑的内容。